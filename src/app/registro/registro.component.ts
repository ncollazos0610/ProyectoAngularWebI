import { Component } from '@angular/core';

@Component({
  selector: 'app-registro',               // Nombre del componente (debe coincidir con lo que usarás en HTML)
  templateUrl: './registro.component.html', // Ruta del archivo HTML
  styleUrls: ['./registro.component.css']   // Ruta del archivo CSS
})
export class RegistroComponent {
  // Aquí irá la lógica del formulario o funciones del componente
}
